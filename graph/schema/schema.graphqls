# enum
enum SearchType {
  USER
}

type SearchResultItemConnection {
  after: String
  before: String
  first: Int
  last: Int
  query: String!
  type: SearchType!
}

type User {
  id: ID!
  name: String!
}